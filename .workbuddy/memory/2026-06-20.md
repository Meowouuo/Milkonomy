# 2026-06-20 工作日志

## 任务：排查缺失物品"蔚蓝长枪" + 合并代码

### 排查结果

- **根因**："蔚蓝长枪"（Azure Spear）被 dashboard 默认的 `banEquipment: true`（排除装备）过滤掉了
- 数据链路完整：`data.json` → `itemDetailMap` → 翻译文件（`result.json`、`milky_trans.json`、`zh-cn.ts`）全部包含该物品
- Azure Spear 的 categoryHrid = `/item_categories/equipment`，Calculator.isEquipment 返回 true，被 `handleSearch()` 中的 `banEquipment` 过滤

### MyDashboard vs Dashboard 代码对比

发现 6 个差异点，其中需要合并的 2 个已执行：

1. ✅ **扩展运算符赋值** — `data.list` → `[...data.list]`（dashboard 2 处均已合并）
   - `leaderboardData.value = [...data.list]`
   - `favoriteData.value = [...data.list]`
   - 确保 API 返回同一份缓存时 Vue 仍能检测到引用变化

2. dashboard 已是更完整的版本（有 `crossStepBalance`、成交量筛选等），myDashboard 缺失这些功能
